import React from 'react';
// import './ourlit-button.css';

// import { OurlitButtonProps } from '@types';

const OurlitButton = ({ text }: { text: string }) => (
    <button className="ourlit-button">{text}</button>
);

export default OurlitButton;
